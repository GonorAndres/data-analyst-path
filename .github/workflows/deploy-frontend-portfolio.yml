name: Deploy Financial Portfolio Tracker

on:
  push:
    branches: [main]
    paths:
      - "projects/05-financial-portfolio-tracker/src/**"
      - "projects/05-financial-portfolio-tracker/public/**"
      - "projects/05-financial-portfolio-tracker/package.json"
      - "projects/05-financial-portfolio-tracker/next.config.js"
      - "projects/05-financial-portfolio-tracker/tailwind.config.ts"
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: prj_sRSJiGCR03kYdWqxPpbXow6BFJ0j

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: projects/05-financial-portfolio-tracker

      - name: Build
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: projects/05-financial-portfolio-tracker

      - name: Deploy to production
        id: deploy
        run: |
          URL=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "url=$URL" >> "$GITHUB_OUTPUT"
          echo "Deployed to: $URL"
        working-directory: projects/05-financial-portfolio-tracker

      - name: Smoke test
        run: |
          sleep 10
          HTTP=$(curl -s -o /dev/null -w "%{http_code}" "https://financial-portfolio-tracker-iota.vercel.app")
          echo "GET / -> HTTP $HTTP"
          if [ "$HTTP" -ge 400 ]; then
            echo "FAIL: site returned HTTP $HTTP"
            exit 1
          fi
          echo "PASS"
