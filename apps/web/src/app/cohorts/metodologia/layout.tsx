import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Methodology — Olist customer retention',
  description: 'Definitions, calculations, data sources, and limitations behind the Olist cohort analysis.',
}

export default function MethodologyLayout({ children }: { children: React.ReactNode }) {
  return children
}
